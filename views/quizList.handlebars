
{{#each quizzes as |quiz| }}
<div class="row mb-4 quiz">
    <div class="col-md-5" >
        <h2 class="purple-box">
            <a href="/quiz/{{quiz.id}}"class="quiz-button">{{quiz.quiz_name}}</a>
        </h2>
    </div>
</div>
{{/each}}